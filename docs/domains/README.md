# Domain Routing

This folder is for lightweight routing notes only. Keep the detailed source-of-truth docs in `docs/` and route to them from `docs/CONTEXT-MAP.md`.

Use this folder when a domain grows enough to need a short `README.md` that explains which existing docs to read first. Do not move or duplicate the large domain documents here during routing cleanup passes.
